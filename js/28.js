// Selectores

const heading = document.querySelector('.heading') // trae hasta un elemento
console.log(heading);
console.log(heading.tagName);
console.log(heading.textContent)
console.log(heading.id)
console.log(heading.classList)

const enlaces = document.querySelectorAll('.navegacion a') // trae todos los elementos que correspondan al selector
console.log(enlaces)

